#ifndef FUNCTION_H
#define FUNCTION_H

/*
    helps avoid the 'unused paremeter' warning that compiler throws
    and sometimes we want to keep parameters to a function for later use
*/
#define UNUSED(x) void(x)

#endif